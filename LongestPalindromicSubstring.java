class Solution{
    public static String longsetPalindromicSubString(String s){
        if (s == null || s.length() < 2) return s;

        boolean[][]dp = new boolean[s.length()][s.length()];
        char[] map = s.toCharArray();
        int maxLen = 0;
        String res = null;

        for (int i = 0; i <s.length(); i++ ){
            
        }
        return res;
    }
}